---
import Link from "./link.astro";

type Props = { page: { title: string; path: string; external?: boolean } };

const { page } = Astro.props;
---

<Link href={page.path} external={page.external}>{page.title}</Link>
